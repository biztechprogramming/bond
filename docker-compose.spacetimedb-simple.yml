version: '3.8'

services:
  spacetimedb:
    image: clockworklabs/spacetime:v2.0.2
    container_name: bond-spacetimedb
    restart: unless-stopped
    ports:
      - "18787:3000"
    volumes:
      - ~/.bond/spacetimedb/data:/var/lib/spacetimedb
    command: start